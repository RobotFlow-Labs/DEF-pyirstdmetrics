# Tasks - DEF-pyirstdmetrics

Granular implementation tasks are tracked in:
- `tasks/INDEX.md` (authoritative status board)
- `tasks/PRD-*.md` (single-task execution units)

Execution policy:
1. Pick the first `NOT_STARTED` task in `tasks/INDEX.md`.
2. Complete implementation and verification for that task.
3. Update task status in `tasks/INDEX.md`.
4. Update `NEXT_STEPS.md` resume pointer.
